name: Move a major tag to the latest release

on:
  push:
    tags:
      - v**

jobs:
  tag:
    name: Move major tag
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0
      - name: Move major tag
        uses: ./
